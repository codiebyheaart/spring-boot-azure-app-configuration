spring:
  application:
    name: azure-appconfig-demo

  cloud:
    azure:
      appconfiguration:
        stores:
          - connection-string: ${APP_CONFIG_CONNECTION_STRING}
logging:
  level:
    com.azure.spring.cloud.appconfiguration: TRACE
    com.azure.data.appconfiguration: DEBUG
